<Window x:Class="MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Wpf_SpaceResection"
        mc:Ignorable="d"
        Title="空间交会SpaceResection" Height="450" Width="800">
    <Window.Resources>
        <ControlTemplate x:Key="DefaultButtonTemplate" TargetType="Button">
            <Border Background="Red" CornerRadius="5" >
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="❤"  VerticalAlignment="Center"/>
                    <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalAlignment}"
                                  VerticalAlignment="{TemplateBinding VerticalAlignment}"/>
                </StackPanel>
                

            </Border>
        </ControlTemplate>
    </Window.Resources>
    <Grid>
        <Button Width="100" Height="40" Content="text" VerticalAlignment="Center" HorizontalAlignment="Center"   Template="{StaticResource DefaultButtonTemplate}"/>
    </Grid>
</Window>
